# Copy to .env and fill in. Only VITE_ vars reach the browser build.
# Set the same value in Cloudflare Pages → Settings → Environment variables.
VITE_QUESTR_SIGN_URL=https://your-lambda-id.lambda-url.eu-west-3.on.aws/
